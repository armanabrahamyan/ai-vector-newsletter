{#- The AI Vector mark, "The Open Block". Derived from docs/brand/aiv-mark.svg,
    which is the single source of truth; regenerate rather than redraw.

    Included by issue.html.j2 and index.html.j2 so the path lives in one place
    in the template layer. fill-rule="evenodd" is load-bearing: it makes the
    second subpath a hole rather than a second block of ink. Colour comes from
    the container via currentColor, which is always --ink -- the accent stays
    spent on the wordmark slash. -#}
<svg viewBox="0 0 100 100" role="presentation"><path fill-rule="evenodd" fill="currentColor" d="M0 0L100 0L100 100L0 100ZM18.37 0L36.37 84.7L100 16.46L100 0L93.47 0L45.63 51.3L34.72 0Z"/></svg>
