## What changes in tomorrow's issue

<!-- The behavioral delta on the product, not the code delta.
     "Nothing — internal only" is a valid answer; say it and move on.
     If reader-visible: staging preview path. -->

## How we know it's better

<!-- Not just different — better. For prompt/model changes: executed eval-gate
     results pasted here (numbers + report path), never a promise. For output-
     shaping changes: staged before/after. For code: tests (count) + how it was
     actually exercised. -->

## If it's wrong

<!-- One line: failure-soft or blocking? Revert = version rollback, or something
     harder? -->
